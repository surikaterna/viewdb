{
  "name": "viewdb-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@8.19.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
