{
  "name": "viewdb-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@8.19.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "2.30.0",
    "turbo": "^2",
    "typescript": "6.0.2"
  }
}
